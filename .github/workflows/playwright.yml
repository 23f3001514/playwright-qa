name: Playwright QA Automation

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: |
          npm install
          npx playwright install --with-deps

      - name: Run Script - 23f3001514@ds.study.iitm.ac.in
        run: node scrape.js
